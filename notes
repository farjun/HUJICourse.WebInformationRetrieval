step 1:

for review in reviewBatch:
    index.proccessReview(review)


Index.flushReviews()

def proccessReview(review):
    Trie.insert(token)
    proccess review...

def flushReviews():
    index.flush()





iter1
iter2

out
while(iter1.isnotEmpty and iter2...)
    sortableNode1 = iter1.next()
    sortableNode2 = iter2.next()
    if(sortableNode1 == sortableNode2 ){
        sortableNode1.merge(sortableNode2)
    }

    if()